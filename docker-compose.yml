services:
  concierge:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./config:/app/config:ro
      - ./checklists:/app/checklists:ro
    env_file:
      - .env
    restart: unless-stopped
